apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spig-starter-kit.fullname" . }}-test-connection"
  labels:
    {{- include "spig-starter-kit.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spig-starter-kit.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
